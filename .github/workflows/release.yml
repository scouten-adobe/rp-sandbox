name: Release-plz

permissions:
  pull-requests: write
  contents: write

on:
  push:
    branches:
      - main

jobs:
  release-plz:
    name: Release-plz
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install fork of release-plz
        uses: taiki-e/cache-cargo-install-action@v2
        with:
          tool: release-plz
          git: https://github.com/scouten-adobe/release-plz.git
          rev: 817bff9abaeb2d8df866a01f62bfe0de04b082fc

      - name: Run release-plz release-pr
        run: |
          release-plz release-pr --git-token ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Run release-plz release
        run: |
          release-plz release --git-token ${{ secrets.RELEASE_PLZ_TOKEN }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}

      # - name: Run release-plz
      # # (Restore this when/if https://github.com/MarcoIeni/release-plz/pull/1657
      # #  is merged.)
      #   uses: MarcoIeni/release-plz-action@v0.5
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
      #     CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
